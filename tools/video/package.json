{
  "name": "yam-plata-video",
  "private": true,
  "type": "module",
  "description": "Renders the daily 'is there palata tomorrow?' vertical video from the live forecast (Playwright + ffmpeg).",
  "scripts": {
    "story": "node render.mjs",
    "preview": "node render.mjs --preview"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
