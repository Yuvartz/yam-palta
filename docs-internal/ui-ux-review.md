# סבב בדיקה ושיפור — Skill Pack (yp-*) · 2026-09-20

מסמך פנימי (לא מפורסם ב-`docs/`). נכתב במהלך הסבב שהוגדר ב-`Skill_Pack/Yam_Palata_App_Skills.md`.

## התקנה
- 12 סקילים נוצרו תחת `.claude/skills/yp-*/SKILL.md` (created 12 · identical 0 · conflict 0).
- אומת: YAML עם `name`+`description` בלבד, שם = תיקייה, ללא אינטרפולציה דינמית, נתיבים בתוך הריפו.
- לא הותקן דבר גלובלית, ללא hooks/MCP/תלויות.

## בסיס (baseline)
- עץ עבודה נקי לפני הסבב (3 קומיטים מקומיים מעל origin, לא נדחפו). שרת פיתוח: `python -m http.server 5050 --directory docs`.
- דפדפן: Browser pane של Claude, אמולציית 375×812 + desktop. קונסול נקי (0 שגיאות) לפני ואחרי.
- נוסחת המדד (`palata.js`: משקלים, עקומות, תקרות, ספים) — **לא שונתה** בסבב הזה. אומת ב-Node: `scoreOf(.05,.02,2,2)=94`, `scoreOf(.65,0,3,3)=74` לפני ואחרי.

## אצוות שינוי

| # | ממצא (נצפה) | סקיל | שינוי | אימות | אי-ודאות |
|---|---|---|---|---|---|
| 1 | רענון רקע שנכשל בזמן שיש נתונים — שקט לגמרי; הפוטר ממשיך להציג "עודכן לאחרונה" | yp-data-state-ux | `load()` catch: אם יש `hourly` → דגל `refreshFailed`, הפוטר מציג "⚠️ הרענון נכשל — מוצגת התחזית מ-HH:MM · נסה שוב"; מתאפס בטעינה מוצלחת | סימולציה: `fetch` → reject, `App.refresh()` → הפוטר השתנה, ההירו נשאר, `App.refresh()` אמיתי החזיר "עודכן לאחרונה" | לא נבדק offline אמיתי של הדפדפן (הכלי לא חוסם רשת) |
| 2 | `toggleScore` ו-`heroDayNav` מחליפים את הצומת הממוקד → פוקוס מקלדת אובד | yp-hebrew-accessibility, yp-navigation-forms | החזרת פוקוס ל-`.hero-score` / לחץ הניווט המתאים (או התאום כשהוא מושבת בקצה הטווח) | JS: אחרי 3 לחיצות ניווט `document.activeElement` הוא `.tl-nav`; תאריך/נקודות נכונים | לא נבדק עם קורא מסך |
| 3 | דיאלוגים: אין נעילת גלילה של הדף מאחור | yp-navigation-forms | `body.modal-open{overflow:hidden}` ב-open/close | JS: המחלקה מתווספת/מוסרת בפתיחה, Escape, "ביטול" | — |
| 4 | טווחי שעות "06:00–09:00" בתוך טקסט RTL עלולים להתהפך (חלון פלטה, טקסט שיתוף, הודעת ערב) | yp-hebrew-accessibility, yp-sea-data-visualization | `<bdi dir="ltr">` בחלון הפלטה; LRI…PDI ב-`shareText` וב-`eveningCopy` (palata.js, טקסט בלבד) | Node: `eveningCopy` מכיל ⁦…⁩; ויזואלית לא נצפה טווח כי אין חלון פלטה בנתוני היום | תצוגה בוואטסאפ/iOS לא נבדקה |
| 5 | טעינת מדוזות ומצוף בונות מחדש את כל ההירו ~1ש' אחרי הציור הראשון → הגלים "קופצים" | yp-motion-design, yp-web-performance | מדוזות: rerender רק אם צ'יפ האזהרה משתנה; מצוף: `patchBuoyLine()` מחליף רק את המשפט | `document.querySelectorAll('.wave-scene').length===1` אחרי טעינה מלאה; הקו של המצוף מופיע | לא נמדד frame pacing במכשיר אמיתי |
| 6 | לולאות אמביינט ממשיכות לרוץ בטאב מוסתר | yp-web-performance, yp-motion-design | `body.bg` ב-visibilitychange → `animation-play-state: paused` לכל שכבות הים/דמויות | JS: המחלקה מתווספת ב-hidden | חיסכון סוללה לא נמדד |
| 7 | reduced-motion לא כיסה: install-tip, מודלים, באנר, פאנלים נפתחים, ברים, אייקון סאונד; transitions על צ'יפים | yp-motion-design | הרחבת החוק; `scroll-behavior:auto` | סטטי בלבד — הכלי לא מאפשר אמולציית prefers-reduced-motion | **לא נבדק ברינדור** |
| 8 | טוסט/nudge ההתקנה קבוע למעלה וכיסה את צ'יפי החופים במובייל | yp-mobile-pwa, yp-ui-design-system | מיקום תחתון עם safe-area, רוחב מלא בטלפון | ויזואלי: הצ'יפים חופשיים; הטוסט לא נבדק בתזמון אמיתי (מופיע מביקור שני) | — |
| 9 | כפתורי הסרת חוף × בגודל 18px; range ללא focus-visible | yp-hebrew-accessibility | `::after` שטח מגע 36px; outline ל-range ולכל הכפתורים החדשים | סטטי (CSS) | — |
| 10 | מפת החום: 84 תאים עם role=img → 84 הכרזות | yp-hebrew-accessibility, yp-sea-data-visualization | תאים `aria-hidden`; כל שורה `role=img` עם סיכום אחד ("% + שיא") | DOM נבדק | לא נבדק בקורא מסך |
| 11 | ציר הזמן בהירו: עמודת "גובה גל" (סה"כ) ליד ספרייט שמייצג גלי רוח — בלי כותרת | yp-sea-data-visualization | שורת כותרות עמודות (שעה · גלי רוח · גובה גל · מדד · מצב) | צילום מסך מובייל | — |
| 12 | "% זמן פלטה" נקרא כהסתברות | yp-sea-data-visualization, yp-product-ux | הערה קצרה מתחת לתחזית: אחוז משעות האור בסף 8.0, לא הסתברות; הסבר "שיא" | ויזואלי | — |

## מסעות שנבדקו (אמולציה, לא iPhone)
- טעינה ראשונה → skeleton → הירו (count-up), 0 שגיאות.
- החלפת חוף מהירה (נואיבה→תל אביב ברצף): הצ'יפ הפעיל, LS ו-ההירו עקביים (guard `loadSeq`).
- הוספת מקום: ערך לא חוקי (lat 95) → שגיאה אינליין, השם נשמר, המודל נשאר פתוח; Escape → נסגר, `inert` הוסר, פוקוס חזר ל"＋ הוסף", `yp-locs` לא השתנה.
- מודל התקנה: פוקוס ראשוני על "הבנתי", Escape סוגר, נעילת גלילה משוחררת.
- העדפת מים 30.5°: תווית, localStorage, הערת "קר מ-30.5°" — המדד לא השתנה (6.7).
- ניווט ימים בציר הזמן ×3: תאריך/נקודות נכונים, פוקוס נשמר, סצנת גלים אחת (לא נבנתה מחדש).
- כשל רענון מדומה → הודעה בפוטר; התאוששות.
- לא בוצע: שליחת דיווח אמיתי, בקשת הרשאת התראות, מנוי Push, גיאולוקציה (הרשאה).

## פתוח / דורש החלטה
- **פרסום**: לא בוצע commit/push בסבב הזה (לפי ההנחיה). השינויים ממתינים בעץ העבודה יחד עם 3 קומיטים קודמים שלא נדחפו.
- **בדיקות שלא זמינות בסביבה**: prefers-reduced-motion אמיתי, offline אמיתי, iOS מותקן, קורא מסך, מדידת ביצועים במכשיר.
- **הצעות מוצר (לא בוצעו, מחוץ להיקף קוסמטי)**: ערכת נושא בהירה לשימוש בחוץ; פרופילים לפי פעילות (שחייה/סאפ/קיאק); אזהרת רוח יבשתית לסאפיסטים; הקשחת צינור ה-Push (אידמפוטנטיות, סנכרון מנויים).
- **באג ידוע שלא טופל**: `israelMidnightUTC` שגוי בתאריכי מעבר שעון (זריחת/שקיעת ירח בלבד, דקורטיבי).

## סבב 2 — בקשות המשתמש (2026-09-20, לילה)

| בקשה | מה נעשה | אימות |
|---|---|---|
| מצלמה עם הטבעת המדד | כפתור 📷 בסרגל → מצלמת המכשיר (`input capture`) → GPS → חוף מוכר עד 40 ק״מ (מדד חי, נטען אם צריך) → קנבס: ציון, דרגה, מים, חוף, שעה, wordmark → שתף (Web Share files) / שמור. בלי מיקום: החוף שנבחר, עם הסבר | הזרקת קובץ תמונה סינתטי ל-input בדפדפן: המודל נפתח, התמונה מורכבת, קישור שמירה קיים, הודעת "לא התקבל מיקום" (הדפדפן חסם GPS) |
| מעבר בין חופים | בלי skeleton: הדף נשאר, מתעמעם, פס התקדמות עליון, הנתונים החדשים נכנסים באנימציה; כישלון → חזרה לחוף הקודם עם toast | JS: `switching`+bar דולקים, ההירו נשאר, כבים כשהנתונים נוחתים; h2 מציג "נואיבה" |
| איחוד מפת חום + תחזית | כרטיס "השבוע · {חוף}" אחד: שורה ליום = שם, רצועת חום, %, שיא, chevron; לחיצה = פירוט שעתי | צילום מסך מובייל |
| שם החוף בכרטיסים | `.h2-loc` בכל כותרת (נתונים, מגמה, שיא, גלים, השבוע, חלון) | ויזואלי |
| התראות "לא עבדו" | קישור "שלח התראת בדיקה" + הסבר מתי נשלחת ומה חסום; **הסיבה האמיתית**: Web Push לא מוגדר (SETUP-PUSH.md) — כרגע רק כשהאפליקציה פתוחה | לא ניתן לבדוק הרשאה כאן (חסומה בדפדפן הבדיקה) |
| אימוג'ים ואייקונים | כל אימוג'י UI (תוויות מדדים, כותרות, צ'יפים, כפתורים, 💡🛟🔥📍＋📲) הוחלף ב-24 אייקוני SVG קו אחידים; פירוט המדד קיבל 4 אייקונים דו-גוניים; אימוג'י הדרגות נשאר בפסק הדין ובהתראות (זה ה"קול") | ויזואלי |
| חצים הפוכים | `‹ ›` הם תווים מראתיים ב-RTL ולכן התהפכו; הוחלפו ב-SVG עם כיוון מפורש (ימני מצביע ימינה = יום קודם) | ויזואלי |
| החלקה כיפית | השורות עוקבות אחרי האצבע (0.4×, התנגדות בקצה), snap-back או מעבר יום | לא ניתן לבדוק מגע בכלי; קוד בלבד |
| "נמדד עכשיו גל…" מיותר | ירד מההירו לשורת המקורות (עם גיל המדידה ומרחק). רק אזהרה "גבוה מהתחזית" נשארת בהירו, כשורה שקטה | ויזואלי |
| מדוזות בולט מדי | ירד מראש ההירו לשורה קטנה מתחת לפסק הדין | ויזואלי |
| ירח וכוכבים | ירח: גאומטריה אמיתית של קו הטרמינטור (חצי ירח = קו ישר, גיבוס נכון), צד מואר לפי waxing; כוכבים: מיקום קבוע ליום (לא מתערבב בכל רינדור) | ויזואלי (הירח היום: 0.55 מואר) |
| צבעים | "כמעט" (#5ec9c2) היה בלתי-נבדל מ"פלטה" (#2dd4bf) → #8ee3c8 מנטה. שאר הפלטה עקבית | ויזואלי במקרא |

## סבב 3 — משוב על פסק הדין ועל "השבוע" (2026-09-20)

| בקשה | מה נעשה | אימות |
|---|---|---|
| אימוג'י השחיין (🏊) בפסק הדין לא הוחלף | 7 איורי SVG משלנו לדרגות (מראה/שמש, גל רגוע, צב, שחיין, רוח+גלים, גל גדול, ענן+ברק), נצבעים בצבע הדרגה עם זוהר עדין ו-bob; משמשים בהירו, ב"מצוין עכשיו", בחלון הפלטה ובכפתורי הדיווח. אימוג'י נשאר רק בטקסט (התראות/שיתוף) | צילום מסך: "גלי עדין" עם שחיין SVG בזהב |
| "השבוע" לא חכם: אותו צבע כל היום, כיוון שמאל→ימין | לפי תכנון אסטרא (Codex): צבע רציף לפי ציון (OKLab בין עוגני הדרגות, `scoreColor`), 14 עמודות שגובהן = ציון (טווח 30→100 על 3→32px כדי שההבדל בין 6.0 ל-7.9 ייראה), ◆ שיא, קו תחתון "עכשיו", דהוי לעבר, נקודה ללילה, מקף לחוסר נתונים, קו מקווקו בסף 8.0; RTL אמיתי (06 מימין) כולל ציר; מקרא = רמפה רציפה + סימנים; הפירוט השעתי מציג כל שעה 06–19 עם ציון, דרגה, מצב | צילום מסך מובייל: גרדיאנט צהוב→מנטה בתוך יום אחד, גבהים משתנים, ציר 06 מימין, רמפה נראית; 0 שגיאות קונסול |

## סבב 4 — איור השחיין וגלי ציר הזמן (2026-09-20)

| בקשה | מה נעשה | אימות |
|---|---|---|
| שחיין "גלי עדין" לא לטעם | עוצב מחדש כקווים נקיים: ראש מלא, גוף אלכסוני בקו עבה, זרוע חתירה קשתית קדימה, שתי טיפות התזה, שני קווי גל (בלי בלוק מים כבד). נבדק בגודל 150px וב-40px | דף תצוגה זמני (נמחק), צילום מסך |
| עמודת "גלי רוח" בציר הזמן — ספרייטי PNG לא יפים | הוחלפו ב-SVG פרוצדורלי: סוול ממולא בגרדיאנט, אמפליטודה/תדירות/קצף לפי גלי הרוח, צבוע בצבע הדרגה; חד בכל צפיפות פיקסלים. הספרייטים הוסרו מה-precache של ה-SW (v8); הקבצים עצמם נשארו בריפו | צילום מסך מובייל: 5 שורות עם גלים שונים, קצף ב-18:00 |
| שיא חום המים בתוך מגמת הטמפ׳ | כרטיס אחד "טמפ׳ המים": גרף + שורה עונתית מתחת; מוצג אם יש לפחות אחד מהם | צילום מסך |

## סבב 5 — איורי דרגות ב-gpt-image-1 + קו ים דו-רכיבי (2026-09-20)

| בקשה | מה נעשה | אימות |
|---|---|---|
| השחיין העליון לא יפה → GPT Image, אסטרא מנסח | אסטרא (Codex) כתב פרומפט ומדריך סגנון (סטיקר וקטורי שטוח, שוליים 15%, צורות עבות ≥40px על 1024, ללא טקסט/רקע/גרדיאנטים). `scripts/gen-tier-art.py` קורא ל-gpt-image-1 (1024², high, רקע שקוף), חותך שוליים, שומר 320px + preview. הופק לכל 7 הדרגות באותה משפחה (צבע לפי דרגה). `tierArt()` משתמש ב-PNG עם נפילה ל-SVG; SW v9 מקדים-מטמין אותם | תצוגה מקדימה של הקבצים + צילום מסך בהירו |
| קו "צורת הים" בציר הזמן — לדייק ולהחכים | שני רכיבים פיזיקליים: סוול ארוך (אמפליטודה מגובה הסוול, אורך גל מהמחזור) + גלי רוח קצרים רוכבים עליו (אמפליטודה מגובה גלי הרוח), קצף רק מעל 0.25 מ׳; סקאלה אנכית קבועה (0.8 מ׳ = אריח מלא) כדי ששורות יושוו ביושר; title עם המספרים | צילום מסך: היום — סוול חלק אחיד; שישי אחה"צ — ריפלים + קצף |

## סבב 6 — חותמת המצלמה, אינטרו, אנליטיקס (2026-09-20)

| נושא | מה נעשה | אימות |
|---|---|---|
| חותמת המצלמה על תמונה אמיתית | פס תחתון כהה יותר; מימין: סטיקר הדרגה + "מדד הפלטה" קטן מעל ציון גדול עם /10, פסק הדין, שורת מטא (חוף · תאריך · שעה · מים); משמאל: לוגו עגול + wordmark + כתובת | צילום התוצאה בגודל מלא על תמונת השקיעה של הבעלים |
| וידאו אינטרו | תויק ב-`files/intro/vidintro.mp4`; הומר ל-`docs/video/intro-480.{mp4,webm}` (88KB/75KB, בלי אודיו) + פוסטר. מוצג **פעם אחת** לכל מכשיר (ועוד פעם אחת בהפעלה הראשונה כמותקן), בכרטיס מעוגל על רקע כהה, "דלג", נגמר → נעלם; לא מוצג ב-reduced-motion, בקישור עמוק, או אם autoplay נחסם; timeout 7 שנ׳ | דפדפן: מתנגן ב-2.16 שנ׳, נעלם בסוף, האפליקציה מתחתיו נטענה במקביל; דגל נשמר |
| אנליטיקס | הוחלט: GoatCounter. מודול `Track` רדום עד ש-`ANALYTICS.code` מוגדר; אירועים: launch (browser/installed × first/returning/regular), installed-daily-active, app-installed, install-tap, beach, day-nav, camera-open/photo, share, notifications, sound, feedback, intro-shown. מדריך ב-`docs-internal/analytics-setup.md`; README עודכן | לא ניתן לבדוק שליחה בלי קוד אתר |

## סבב 7 — כרטיס החותמת (2026-09-20)

| בקשה | מה נעשה | אימות |
|---|---|---|
| למקם את מקבץ המידע איפה שרוצים | התצוגה במודל היא canvas חי; גרירה (pointer events, גם מגע) מזיזה את הכרטיס, מוגבל לגבולות התמונה; ה-JPEG נבנה מחדש בשחרור | אירועי pointer מדומים → הכרטיס זז לפינה/למרכז, הקובץ יוצא נכון |
| "מדד הפלטה" בטורקיז עם זוהר, ברוח האפליקציה | כרטיס זכוכית כהה מעוגל עם צל, פס טורקיז→ירוק (כמו סמן ה-h2), כותרת טורקיז זוהרת, ציון זוהר בצבע הדרגה + /10 + סטיקר, פסק דין, מטא (מותאם לרוחב), שורת מותג עם הלוגו | 3 קבצי דוגמה נשלחו לבעלים |
| באג שנמצא אגב | `.intro{display:flex}` ניצח את `hidden` → שכבת האינטרו כיסתה את האפליקציה בכל טעינה אחרי הראשונה. תוקן: `.intro[hidden]{display:none}` | `getComputedStyle(intro).display === 'none'` בטעינה חוזרת |

## סבב 8 — דיווח מהאייפון: "לא הצלחנו לעבד את התמונה" (2026-09-20)

צילום המסך תויק ב-`files/reports/2026-09-20-iphone-camera-fail.jpeg`. ההודעה הישנה לא אמרה מה נכשל, ולכן אי אפשר לקבוע בוודאות; החשודים הסבירים ב-Safari של iOS: (1) פענוח קובץ ענק/HEIC מהמצלמה דרך `<img>` (12–48MP), (2) `roundRect` שחסר ב-Safari לפני 16.4, (3) canvas גדול. תיקונים: פענוח דרך `createImageBitmap` עם הקטנה בזמן הפענוח (נופל ל-`<img>` רק אם אין), polyfill ל-`roundRect`, `photoDims` תומך ב-ImageBitmap, והודעת שגיאה שמציגה את הסיבה בפועל + "צלם שוב" + אירוע `camera-error`. SW v11.
אימות מקומי: תמונה סינתטית 4000×3000 → canvas 1600×1200, שיתוף פעיל; קובץ פגום → הודעה עם הסיבה. **צריך בדיקה חוזרת באייפון** — אם עדיין נכשל, הטקסט הקטן במודל אומר למה.

## סבב 9 — האייפון עדיין נכשל; ייעוץ אסטרא (2026-09-20)

- **מסקנה על "לא עובד" השני:** v11 היה באוויר, אבל הבעלים בדק ב-Safari (לא מותקן) ו-GitHub Pages שולח `max-age=600`; ה-SW ביקש navigation עם `fetch(req)` רגיל ולכן קיבל דף בן עד 10 דקות. תוקן: `fetch(req, {cache:"no-cache"})` ל-navigations (SW v12).
- **אסטרא (Codex), דירוג סיבות ל-iOS:** (1) לחץ זיכרון בפענוח 24–48MP / HEIC — `createImageBitmap` + base64 מוסיפים באפרים; (2) canvas ריק/`toBlob` null בשקט תחת לחץ זיכרון; (3) תאימות ציור (`roundRect` נייטיב מ-16, לא 16.4 — כבר polyfilled). geolocation/fonts/SW לא סבירים (נתפסים). המלצה: `<img>` קודם (Safari מדגים-מטה JPEG ענק בעצמו), ציור פעם אחת ל-canvas מוגבל שמשמש כתמונת העבודה, בלי ImageBitmap/base64 כברירת מחדל, בדיקת context ופיקסל, deadlines.
- **יושם:** `loadImage` חדש בסדר img→canvas≤1600 (בדיקת פיקסל לא-ריק) → ImageBitmap → data URL, כל שלב עם timeout 20 שנ׳ ורישום מה נוסה; `handleCameraFile` בשלבים (decode/locate/assets/render/encode) — איתור ונכסים לא קטלניים, ציור הכרטיס עם נפילה לכיתוב פשוט, בדיקת null מ-toBlob; הודעת שגיאה = שלב + סיבה + סביבה (build, iOS/Safari, מותקן/דפדפן, סוג וגודל הקובץ). `BUILD="v12"` מוצג בשורת הסביבה כדי לדעת איזו גרסה נבדקה.
- אימות מקומי: 4032×3024 JPEG → canvas 1600×1200, שיתוף פעיל; קובץ פגום → "decode: cannot decode untyped — img:… | bitmap:… | dataurl:…" + סביבה.
