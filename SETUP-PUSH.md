# הפעלת התראות רקע (Web Push) — Cloudflare Worker, חינם, ~30 דקות

בלי זה: התראות עובדות רק כשהאפליקציה פתוחה. עם זה: "ים פלטה בתל אביב!" מגיע למסך הנעול,
כולל אייפון (iOS 16.4+, אחרי "הוסף למסך הבית").

מה יש בקוד כבר: `workers/push/` (ה-Worker: API + בדיקה כל 15 דק׳ + שליחה), `docs/index.html`
(הרשמה, כיבוי, בדיקה מהשרת, סנכרון אוטומטי בהחלפת חוף), `docs/sw.js` (קבלה, לחיצה → החוף הנכון,
רוטציה של מנוי). המדיניות (`workers/push/src/policy.js`): התראת "התחיל פלטה" כשהמדד עובר 8.0 ויש
לפחות עוד שעה רגועה, עד 2 ביום; שדרוג "דלוקס" פעם ביום; תצוגה מקדימה למחר בבוקר ב-19:00;
שקט 21:00–06:00; אין כפילויות גם אם הבדיקה רצה פעמיים.

## מה שאתה עושה (פעם אחת)

1. **חשבון Cloudflare** (חינם): https://dash.cloudflare.com/sign-up
2. בטרמינל, בתיקיית הפרויקט:
   ```bash
   cd weather-app/workers/push
   npm install
   npx wrangler login
   ```
   (נפתח דפדפן — אשר.)
3. **מאגר המנויים** (KV):
   ```bash
   npx wrangler kv namespace create SUBS
   ```
   הפלט מכיל `id = "…"`. הדבק אותו ב-`wrangler.toml` בשני המקומות (`id` ו-`preview_id`) במקום `REPLACE_WITH_KV_NAMESPACE_ID`.
4. **סודות** (המפתח הפרטי מ-`VAPID-KEYS.local.txt` — לא נכנס ל-git לעולם):
   ```bash
   npx wrangler secret put VAPID_PRIVATE_KEY
   npx wrangler secret put VAPID_SUBJECT
   ```
   ל-`VAPID_SUBJECT` הזן `mailto:yuvalartzi@gmail.com`.
5. **פריסה**:
   ```bash
   npx wrangler deploy
   ```
   הפלט מסתיים בכתובת כמו `https://yam-palata-push.<שם>.workers.dev`. **תגיד לי אותה** — אני מציב אותה
   ב-`PUSH.apiUrl` (index.html) וב-`PUSH_API` (sw.js), דוחף, וזהו.
6. בדיקה: `https://<הכתובת>/health` צריך להחזיר `{"ok":true,"subscribers":0,"configured":true}`.

## איך בודקים שזה באמת עובד
1. בטלפון: פתח את האפליקציה (באייפון — מהאייקון במסך הבית), לחץ "🔔 הפעל התראות", אשר.
2. הכפתור צריך להפוך ל-"✅ התראות פעילות ל<חוף> — גם כשהאפליקציה סגורה". אם כתוב "ההרשמה לשרת נכשלה" — לחץ שוב ותצלם.
3. לחץ "שלח התראת בדיקה מהשרת", נעל את המסך. ההתראה מגיעה תוך שניות דרך אפל/גוגל, לא מהדפדפן.
4. לחיצה על ההתראה פותחת את האפליקציה על החוף הנכון.
5. `npx wrangler tail` בטרמינל מראה כל ריצה של הבדיקה: `cron … subs=1 beaches=1 sent=0`.

## אם מחליפים דומיין
ב-`wrangler.toml` הוסף את הדומיין החדש ל-`ALLOWED_ORIGINS` ועדכן `APP_URL`, ואז `npx wrangler deploy` שוב.
(סקריפט `scripts/set-domain.py` לא נוגע ב-Worker בכוונה — זה שינוי של שורה אחת שם.)

## פרטיות
נשמרים: כתובת המנוי של הדפדפן (מחרוזת אקראית של אפל/גוגל), שם וקואורדינטות של החוף שנבחר, ומצב ההתראה
האחרון. לא נשמרים: מיקום המשתמש, זהות, מכשיר. כיבוי מהאפליקציה מוחק את הרשומה. מנוי שאפל/גוגל ביטלו
(404/410) נמחק אוטומטית בבדיקה הבאה.
