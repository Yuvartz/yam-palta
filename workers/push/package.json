{
  "name": "yam-palata-push",
  "private": true,
  "type": "module",
  "description": "Yam Palata push backend: subscription store + 15-minute palata evaluator + Web Push sender (Cloudflare Worker, free tier)",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test test/policy.test.mjs"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "^2.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
